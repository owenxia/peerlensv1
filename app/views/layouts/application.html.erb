<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->

		<title>Peerlensv1</title>
		<%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
		<%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
		<%= csrf_meta_tags %>
		<link href='http://fonts.googleapis.com/css?family=Noto+Serif:400,700' rel='stylesheet' type='text/css'>
		<link href='http://fonts.googleapis.com/css?family=Open+Sans:700,300,400' rel='stylesheet' type='text/css'>
	</head>

<body>
<div class="container">

	<nav class="navbar navbar-default">
		<div class="container-fluid">
		  <div class="navbar-header">
		    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
		      <span class="sr-only">Toggle navigation</span>
		      <span class="icon-bar"></span>
		      <span class="icon-bar"></span>
		      <span class="icon-bar"></span>
		    </button>
		    <a class="navbar-brand" href="/">Peerlens</a>
		  </div>
		  <div id="navbar" class="navbar-collapse collapse">
		    <ul class="nav navbar-nav">
		      <li><%= link_to 'Annotations', annotations_path %></li>
		      <li><%= link_to 'External Page', new_annotation_path %></li>
		      <li><%= link_to 'Notebooks', notebooks_path %> </li>
		      <li><%= link_to 'Create a Notebook', new_notebook_path %></li>
			  <li><%= link_to 'Top Content', top_path %></li>
			  <li><%= link_to 'Users', users_path %></li>
			  <li>
			  	  <% if user_signed_in? %> 
					<%= link_to 'Edit profile', edit_user_registration_path %>
				  <% end %>
			  </li>
			  <li>
				  <% if user_signed_in? %> 
					<%= link_to 'Log out', destroy_user_session_path, method: :delete %>
				  <% else %>
	     			<%= link_to 'Log in', new_user_session_path %> 
				  <% end %>
			  </li>
			  <li>
			  	  <% if !user_signed_in? %>
				 	<%= link_to 'Register', new_user_registration_path  %> 
				  <% end %>
			  </li>
		  </div><!--/.nav-collapse -->
		</div><!--/.container-fluid -->
	</nav>


	<div class="alert alert-success" role="alert">

		<p><%= notice %></p>
		<p><%= alert %></p>
		

		<p>
			<% if user_signed_in? %> 
				<span class="glyphicon glyphicon-user"></span> <%= current_user.email %>
			<% end %>
		</p>
		<p>
			<span class="glyphicon glyphicon-lock"></span>

			<% if session[:name] %>
				<p>You're signed in as <%= session[:name] %></p>
				<%= link_to 'Sign out', signout_path %>
			<% else %>
				<%= link_to 'Sign in with Twitter', '/auth/twitter' %>
			<% end %>

		</p>
	</div>

	<%= yield %>



</div><!-- container -->





</body>
</html>
