<%= form_for @annotation do |f| %>
	<%= f.label :quote %><br>
	<%= f.text_area :quote %><br>

	<br>

	<%= f.label :note %><br>
	<%= f.text_area :note %><br>

	<br>

	<%= f.collection_select(:notebook_id, current_user.notebooks.all, :id, :name) %><br>

	<br>
	
	<%= f.submit %>
<% end %>