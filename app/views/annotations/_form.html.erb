<%= form_for :annotation, url: annotations_path do |f| %>
	<%= f.label :quote %><br>
	<%= f.text_area :quote %><br>

	<br>

	<%= f.label :note %><br>
	<%= f.text_area :note %><br>

	<br>

	<%= f.submit %>

<% end %>