<p><strong><%= comment.name %></strong></p>
<p><%= comment.body %></p>
<p><%= time_ago_in_words(comment.created_at) %> ago</p>
<p><%= link_to 'Edit', edit_annotation_comment_path(comment.annotation, comment) %> | <%= link_to 'Delete', [comment.annotation, comment], method: :delete, data: { confirm: 'Are you sure?'} %></p>