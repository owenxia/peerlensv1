<p><strong><%= comment.name %></strong></p>
<p><%= comment.body %></p>
<p><%= time_ago_in_words(comment.created_at) %> ago</p>
<p>
	<% #if can? :update, comment %>
		<%= link_to 'Edit', edit_annotation_comment_path(comment.annotation, comment) %> 
	<% #end %>
	<% #if can? :destroy, comment %>
	<%= link_to 'Delete', [comment.annotation, comment], method: :delete, data: { confirm: 'Are you sure?'} %>
	<% #end %>
</p>